import React, {useState, useEffect, useRef} from 'react'


 const Typewriter  = ({text}) => {
    //  const index = useRef(0)

    // const [inittext, changedtext] = useState('')
    // useEffect (() => {
    //   setInterval(() => {
    // changedtext((value) => value + text.)
    //   },2000)
    // }, [inittext])
return(
    <div>
        <p> {text}</p>
    </div>
)

}
export default Typewriter
